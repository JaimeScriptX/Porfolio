---
import Button from '../components/Button.astro';
import Layout from '../layouts/Layout.astro';
import Icon from '../components/Icon.astro';
import Pill from '../components/Pill.astro';
---

<Layout documentTitle="Jaime García">
  <section class="container" style="z-index: 1; position: relative;">
    <h1>
      <span class="intro-welcome">Hey, mi nombre es <br></span>
      <span class="intro-name text-swipe-animation">Jaime García</span>
    </h1>
    <div class="roles">
      <Pill><Icon icon="code" size="1.33em" />Desarrollador Full Stack Junior</Pill>
    </div>
    <p class="desc">
      Desarrollador apasionado, con más de un año de experiencia, con experiencia en JavaScript, React y Node.
      <br class="desc-break" />  Transformo desafíos en líneas de código y disfruto construyendo soluciones impactantes.
    </p>

    <Button href="/projects" variant="outline" size="lg" colorScheme="green">
      Mira mis proyectos
      <svg style="margin-left: 5px" stroke="currentColor" fill="currentColor" stroke-width="0" viewBox="0 0 20 20" height="1em" width="1em" xmlns="http://www.w3.org/2000/svg"><path fill-rule="evenodd" d="M7.293 14.707a1 1 0 010-1.414L10.586 10 7.293 6.707a1 1 0 011.414-1.414l4 4a1 1 0 010 1.414l-4 4a1 1 0 01-1.414 0z" clip-rule="evenodd"></path></svg>
    </Button>
  </section>

  <canvas id="waves"></canvas>
</Layout>

<style lang="scss">
  $waves-opacity: 0.5;

  @keyframes fadeInWaves {
    0% { opacity: 0 }
    100% { opacity: $waves-opacity }
  }

  canvas#waves {
    width: 100%;
    height: 100vh;
    animation: fadeInWaves 2s;
    background: transparent;
    opacity: $waves-opacity;
    left: 0;
    position: absolute;
    right: 0;
    top: 0;
    overflow: hidden;
    pointer-events: none;
  }

  h1 {
    line-height: 2.5rem;
    margin: 1rem 0;
  }

  .intro-welcome {
    color: var(--color-green);
    font-family: var(--font-family-IBM-Plex-Mono);
    font-weight: 600;
    font-size: var(--font-size-base);
  }

  .intro-name {
    font-family: var(--font-family-Mukta);
    font-size: var(--font-size-14xl);
    overflow: hidden;
    display: inline-block;
    padding: 10px 0;
    color: #ffffff;
  }

  .intro {
    color: var(--color-silver-300);
    font-size: var(--font-size-11xl);
    line-height: 3.25rem;
  }

  .desc {
    color: var(--color-silver-100);
    font-size: var(--font-size-lg);
    line-height: 1.5em;
    margin: 1.5rem 0;
  }

  @media screen and (max-width: 980px) {
    h1 {
      line-height: 1.75rem;
      margin: 0.5rem 0;
    }

    .intro-name {
      font-size: var(--font-size-10xl);
      line-height: 3rem;
    }

    .intro {
      font-size: var(--font-size-3xl);
      line-height: 2rem;
      margin: 0.5rem 0;
    }

    .intro-break {
      display: none;
    }

    .desc-break {
      display: none;
    }
  }
</style>

<script>
  import "../js/wave.js";
</script>